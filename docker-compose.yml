version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    volumes:
      - db_data:/app/data
    restart: unless-stopped

volumes:
  db_data:
